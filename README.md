# putnbr_base
putnbr_base is a simple C utility function that receives a number in decimal (base 10) and prints it converted into another numeral system, defined by the characters in the base string. The function supports any valid base (binary, octal, hexadecimal, custom symbols, etc.), as long as the base string is valid.
